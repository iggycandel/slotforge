/**
 * Edge Runtime stub for @clerk/shared's #safe-node-apis subpath import.
 * The APIs wrapped here (fs, path, os, etc.) are not available in Edge Runtime,
 * but Clerk gracefully degrades when they're absent.
 */
module.exports = {}
